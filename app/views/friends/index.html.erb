<p style="color: green"><%= notice %></p>

<h1>Friends</h1>

<div id="friends">
  <% @friends.each do |friend| %>
    <% if @friends.blank? %>
      <p>You are a looser with no friends</p>
      <%else%>
      <%=render friend%> 
   <%end%>

    <p>
      <%= link_to "Show this friend", friend %>
    </p>
  <% end %>
</div>

<%= link_to "New friend", new_friend_path %>
